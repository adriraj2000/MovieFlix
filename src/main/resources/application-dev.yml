# Development profile configuration
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  devtools:
    restart:
      enabled: true

logging:
  level:
    root: INFO
    com.movieflix: DEBUG
    org.springframework: INFO
    org.hibernate.SQL: DEBUG

server:
  port: 8080
